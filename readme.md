simple python based cli inventory management system. It allows users to add, view, update, and delete inventory items through a command-line interface.

To run the application, 
1. Ensure you have Python installed on your system. 
2. Activate your virtual environment if you are using one.
3. Execute the script using the command `python main.py` in your terminal.